<h2><%= @project %></h2>

<table border>
  <tr>
    <th>Datetime</th>
    <th>Duration</th>
  </tr>

  <% @jobs.each do |job| %>
    <tr>
      <td><%= job.created_at %></td>
      <td><%= job.duration %></td>
    </tr>
  <% end %>
</table>
