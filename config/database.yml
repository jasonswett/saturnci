default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: saturn
  host: postgres
  port: 5432

development:
  <<: *default
  database: saturn_development

test:
  <<: *default
  database: saturn_test

production:
  <<: *default
  database: saturn_production
  username: saturn
  password:
